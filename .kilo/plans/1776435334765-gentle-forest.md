# Run Tests

## Status: Ready to Execute

## Test Files Found

### 1. test_allocator.py (26 lines)
Tests for counselor allocation priority scoring:
- `test_compute_priority_score()` - CRI calculation with trends
- `test_determine_assignment()` - Assignment logic (RED/YELLOW/GREEN)

### 2. test_risk_engine.py (55 lines)
Tests for risk engine calculations:
- `test_compute_phq9_total()` - PHQ-9 scoring
- `test_normalize_behavioral()` - Behavioral factors
- `test_calculate_cri()` - Combined Risk Index
- `test_determine_risk_level()` - Risk level determination
- `test_compute_trend()` - Trend computation
- `test_generate_reasoning()` - Reasoning generation

### 3. modules/test_session.py (51 lines)
Tests for session schemas:
- `test_session_note_validation()` - Session note creation
- `test_session_outcome_validation()` - Outcome validation
- `test_override_validation()` - Override validation

## How to Run Tests

```bash
cd apps/api
uv sync --group dev    # Install dev dependencies first
uv run pytest -v        # Run all tests
```

## Expected Results

| Test File | Tests | Status |
|-----------|-------|--------|
| test_allocator.py | 2 | ✅ Unit tests present |
| test_risk_engine.py | 7 | ✅ Unit tests present |
| modules/test_session.py | 3 | ✅ Schema validation |

## Notes
- Tests use `pytest` and `pytest-asyncio` (in dev deps)
- Uses Pydantic validation testing
- No database required (pure unit tests)
