# =====================================================
# StillMind — Production Maintenance Workflow
# =====================================================

# --- 1. NORMAL UPDATE (No DB Changes) ---
# Use this when you just change CSS, HTML, or logic
cd /opt/stillmind
sudo bash scripts/cloud-sync.sh

# --- 2. DATABASE SCHEMA UPDATE ---
# Use this when you add a new column or table to the models
# 1. Locally:
#    cd apps/api
#    uv run alembic revision --autogenerate -m "add some feature"
#    git add . && git commit -m "feat: db update" && git push
#
# 2. On Server:
#    sudo bash scripts/cloud-sync.sh
#    (It will automatically run 'alembic upgrade head' safely)

# --- 3. MONITORING ---
# See logs in real-time
sudo docker compose logs -f

# --- 4. ACCESS ---
# Platform: https://stillmind.civiclens.space
# API Docs: https://stillmind.civiclens.space/api/v1/docs
